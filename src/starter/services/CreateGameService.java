package services;

import requests.CreateGameRequest;
import responses.CreateGameResponse;

/**
 * Service for handling requests to make a new chess game
 */
public class CreateGameService {
    /**
     * Creates a chess game
     * @param r a request for creating the game
     * @return a response to the request, (failed or not failed)
     */
    public CreateGameResponse createGame(CreateGameRequest r) {return null;}
}
