package services;

import requests.JoinGameRequest;
import responses.JoinGameResponse;

/**
 * Service that handles request to join a chess game
 */
public class JoinGame {

    /**
     * Method to join a chess game
     * @param r a request for clearing
     * @return a response to the request, (failed or not failed)
     */
    public JoinGameResponse join(JoinGameRequest r) {return null;}
}
